version: 2

project_name: manapool-tools

# Disable binary builds since this is a library
builds:
  - skip: true

release:
  github:
    owner: repricah
    name: manapool-tools
  draft: false
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
